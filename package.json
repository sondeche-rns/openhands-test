{
  "name": "wisp-mapping",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint web/**/*.js web/**/*.mjs tests/**/*.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "vitest": "^2.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
